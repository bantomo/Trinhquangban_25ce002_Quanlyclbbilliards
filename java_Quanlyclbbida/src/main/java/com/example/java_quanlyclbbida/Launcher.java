package com.example.java_quanlyclbbida;

import javafx.application.Application;

/**
 * Launcher class - Điểm khởi đầu của ứng dụng
 * Sử dụng class này để chạy ứng dụng JavaFX
 * (Cần thiết để tránh lỗi module khi chạy từ JAR)
 */
public class Launcher {
    public static void main(String[] args) {
        Application.launch(BidaApplication.class, args);
    }
}
